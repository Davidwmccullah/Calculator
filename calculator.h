void operationChoice(void); //Scans in value one to six, and performs the given choice.

void exponent(void); //Scans in base and exponent, then calculates and outputs power. (1 digit max per scan.)

void multiplication(void); //Scans in factors, then calculates product. (1 digit max per scan.)

void division(void); //Scans in divisor and dividend, then calculates quotient and remainder (1 digit max per scan.)

void addition(void); //Scans in two addends, then calculates  sum (1 digit max per scan.)

void subtraction(void); //Scans in minuend and subtrahend, then calculates  difference (1 digit max per scan.)
